npx gitignore node

git init

git checkout -b main

npm init -y

ctrl + k (clean consule)

npx eslint --init {
    problems,
    common js,
    none of these,
    no (typescript),
    node (select w. spaceb),
    JSON,
    yes
}

npm i -D nodemon

3rd btn left -> "project xyz", 
ctrl + enter

gid add .
git commit -m projectXyz

npm i express cors dotenv 

"scripts" {
"test": "echo \"Error: no test specified\" && exit 1"
-->
"start": "node index.js",
"server": "nodemon index.js"

create index.js file
console.log("web 39 rulez")
npm start

npm run server


! copy paste package.json ok, but must run npm i
! 